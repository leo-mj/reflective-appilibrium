# Upper bounds, not just lower ones: CI installs fresh on every run, so an
# unbounded requirement means a breaking upstream release turns into a red build
# on an unrelated PR. Bounds are set at the next major of what is known to work;
# raise one deliberately, with a test run, rather than by accident.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
openai>=1.50,<3.0
anthropic>=0.49,<1.0
pydantic-settings>=2.5,<3.0

# Formal RE model. Pinned to the 1.x line these schemas were written against.
theodias>=1.0.0,<2.0
rethon>=1.0.0,<2.0

# Test and audit tooling.
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<2.0
pytest-cov>=5.0,<8.0
httpx>=0.27,<1.0
pip-audit>=2.7,<3.0
